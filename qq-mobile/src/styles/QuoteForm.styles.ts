import { StyleSheet } from "react-native";

export const QFStyles = StyleSheet.create({
    formImage: {
        maxHeight: '50%',
        maxWidth: 300
    },
    fields: {
        maxHeight: '50%'
    }
})